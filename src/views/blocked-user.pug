include base
block content
    ul.user-lists
        each user in users
            li.user-lists__list
                a(href=`/users/${user._id}`)
                    img(src=`/${user.avatarUrl}`)
                    span=user.name 
        else
            span.errorMsg 🙅 Sorry nothing found.